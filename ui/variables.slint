// Variables & UI States

export global Variables {
    // UI related variables
    // Whether dark mode is enabled or not
    in-out property <bool> dark-mode: false;

    // Runtime variables
    // Current window title
    in-out property <string> current-title: "PicGoX";
    // Current image storage provider
    in-out property <string> current-storage: "null";
}
